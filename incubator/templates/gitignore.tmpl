# Python
__pycache__/
*.py[cod]
*.so
*.egg-info/
.eggs/
build/
dist/

# Poetry / virtualenvs
.venv/
.env

# Editor / OS
.vscode/
.idea/
.DS_Store
Thumbs.db

# Apiary state needs no rules here: this repo carries no local .apiary/ dir.
# Its scribe notes, budgeter data and so on live centrally under the main
# apiary's .repos/<name>-<id>/ store. The per-repo apiary install appends the
# .claude/ rules this repo does need (launcher and settings ignored, repo-owned
# slash commands still trackable) to the bottom of this file.
