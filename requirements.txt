# Third-party Python dependencies for claude-apiary.
# Canonical source: pyproject.toml — this file is kept as a pip-only fallback.
# Project is primarily stdlib-only (see CLAUDE.md / docs/standards/code-style.md).
# Pinned to version RANGES, not exact versions, per the portability epic (T5).

# Test runner — only required for running the test suites.
pytest>=8.0,<9.0

# Coverage reporting. Not needed for a plain `pytest` run: `--cov` is
# deliberately absent from addopts, so this is only pulled in when you ask
# for `pytest --cov`.
pytest-cov>=5.0,<8.0
