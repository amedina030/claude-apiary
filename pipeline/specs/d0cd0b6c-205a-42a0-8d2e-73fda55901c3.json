{
  "goal": {
    "problem": "Ideas and deferred work live scattered across conversations and scribe notes with no staging area, status tracking, or promotion gate before entering the pipeline intake.",
    "solution": "Add a file-based ticket management layer with a backlog directory for drafts, a markdown board for status tracking, and CLI tools to draft and promote tickets into the pipeline.",
    "value": "Pipeline users get a structured path from idea to execution \u2014 drafts are reviewable before committing resources, and board.md provides at-a-glance visibility into all ticket states."
  },
  "shape": {
    "components": [
      {
        "name": "pipeline/backlog/",
        "description": "Directory holding draft ticket JSON files with human-readable filenames (slugified title). Uses the same schema as intake files but without a UUID id field \u2014 the id is assigned on promotion."
      },
      {
        "name": "pipeline/draft_ticket.py",
        "description": "CLI script that creates a backlog JSON from --title/--problem/--description/--scope args or from a scribe note via --from-todo <id>. Writes to pipeline/backlog/<slug>.json and appends a row to board.md with status 'backlog'."
      },
      {
        "name": "pipeline/promote.py",
        "description": "CLI script that validates a backlog ticket against validate_intake.py rules, assigns a UUID, copies to pipeline/intake/<uuid>.json, removes the backlog file, and updates the board.md row from 'backlog' to 'ready'."
      },
      {
        "name": "pipeline/board.md",
        "description": "Markdown table tracking all tickets through statuses (backlog, ready, running, done, failed) with columns for slug, title, status, intake UUID, and notes. Updated by draft_ticket.py, promote.py, and run.py."
      },
      {
        "name": "pipeline/run.py (modified)",
        "description": "Existing orchestrator updated to set board.md status to 'running' at pipeline start and 'done' or 'failed' at pipeline end, keyed by intake UUID."
      }
    ],
    "integration_point": "Sits upstream of the existing pipeline/create_intake.py \u2192 run.py flow, replacing direct intake creation with a draft \u2192 promote \u2192 run lifecycle",
    "pattern": "Follows the existing create_intake.py pattern: CLI with argparse, --from-todo for scribe integration, JSON file output, validation via validate_intake.py, sys.exit(1) on error",
    "data_flow": "CLI args or scribe note \u2192 draft_ticket.py \u2192 pipeline/backlog/<slug>.json + board.md row(backlog) \u2192 promote.py \u2192 pipeline/intake/<uuid>.json + board.md row(ready) \u2192 run.py \u2192 board.md row(running\u2192done/failed)",
    "dependencies": "pipeline/validate_intake.py (reused for promotion validation), scribe/notes.py (for --from-todo lookups), pipeline/run.py (for board status updates), Python 3 standard library (argparse, json, uuid, re, datetime)"
  },
  "behavior": {
    "input": "draft_ticket.py: --title, --problem, --description, --scope, --context (optional), --from-todo <note_id> (optional). promote.py: positional arg <slug> (backlog filename without .json). run.py: existing intake JSON path (unchanged).",
    "processing": "draft_ticket.py: (1) parse args, (2) if --from-todo fetch note content via scribe/notes.py get <id> and use as default description, (3) slugify title to filename, (4) write JSON to pipeline/backlog/<slug>.json with fields title/problem/description/scope/context/source/created_at, (5) append row to board.md with status backlog. promote.py: (1) read pipeline/backlog/<slug>.json, (2) assign UUID as id field, (3) validate via validate_intake.py logic, (4) on pass write to pipeline/intake/<uuid>.json, (5) delete backlog file, (6) update board.md row status from backlog to ready and fill in UUID column. run.py changes: (1) after intake validation passes update board.md row matching UUID to running, (2) after final stage update row to done or failed.",
    "output": "draft_ticket.py prints backlog file path on success. promote.py prints intake file path and UUID on success. run.py prints existing output plus board status updates. board.md is a persistent markdown table file.",
    "error_cases": [
      {
        "trigger": "draft_ticket.py: --from-todo references a note ID that does not exist",
        "behavior": "Print 'Error: note <id> not found' to stderr and exit 1"
      },
      {
        "trigger": "draft_ticket.py: required args (--title, --problem, --scope) missing and no --from-todo provided",
        "behavior": "Argparse prints usage error and exits 2"
      },
      {
        "trigger": "draft_ticket.py: slugified filename collides with existing backlog file",
        "behavior": "Print 'Error: backlog ticket <slug>.json already exists' to stderr and exit 1"
      },
      {
        "trigger": "promote.py: slug does not match any file in pipeline/backlog/",
        "behavior": "Print 'Error: backlog ticket <slug>.json not found' to stderr and exit 1"
      },
      {
        "trigger": "promote.py: backlog ticket fails validate_intake.py validation rules",
        "behavior": "Print validation errors to stderr and exit 1 without creating intake file or modifying board.md"
      },
      {
        "trigger": "run.py: board.md does not exist or has no row matching the intake UUID",
        "behavior": "Skip board update silently \u2014 board tracking is optional and must not block pipeline execution"
      }
    ],
    "edge_cases": [
      {
        "condition": "Title contains special characters, unicode, or very long text when slugified",
        "behavior": "Slugify strips non-alphanumeric chars except hyphens, lowercases, truncates to 60 chars, and deduplicates consecutive hyphens"
      },
      {
        "condition": "board.md does not exist when draft_ticket.py runs for the first time",
        "behavior": "Create board.md with markdown table header row before appending the first entry"
      },
      {
        "condition": "promote.py is called on a ticket that was already promoted (file missing from backlog)",
        "behavior": "Print 'Error: backlog ticket <slug>.json not found' to stderr and exit 1 \u2014 same as missing file case"
      },
      {
        "condition": "Multiple pipelines running concurrently update board.md",
        "behavior": "Each update reads the full file, modifies the target row, and writes back atomically. Concurrent writes may clobber \u2014 acceptable for a lightweight file-based system"
      },
      {
        "condition": "--from-todo note content is too short to meet the 20-char minimum for problem or description",
        "behavior": "draft_ticket.py writes the draft as-is; validation happens at promote time, not draft time"
      }
    ]
  },
  "boundaries": {
    "in_scope": [
      "pipeline/backlog/ directory creation and JSON schema",
      "pipeline/draft_ticket.py CLI with --from-todo support",
      "pipeline/promote.py CLI with validation and UUID assignment",
      "pipeline/board.md markdown table creation and updates",
      "pipeline/run.py modifications for board.md status updates at start and end",
      "Slug generation from ticket titles"
    ],
    "out_of_scope": [
      {
        "item": "Modifying existing stage scripts (auto_refine.py, auto_plan.py, executor.py, auto_harden.py, approval.py)",
        "reason": "Task scope explicitly excludes changes to existing stage scripts"
      },
      {
        "item": "Replacing or deprecating create_intake.py",
        "reason": "draft_ticket.py + promote.py is a parallel path; create_intake.py remains valid for direct intake creation"
      },
      {
        "item": "Database or server-based tracking",
        "reason": "Project convention is file-based and minimal; board.md is a markdown table, not a database"
      },
      {
        "item": "Concurrent write safety beyond simple read-modify-write",
        "reason": "File-based system accepts the small risk of concurrent clobber; adding file locking is overengineering for this use case"
      },
      {
        "item": "Web UI or interactive board view",
        "reason": "board.md is readable in any text editor or GitHub; no UI layer needed"
      },
      {
        "item": "Automatic promotion or scheduling of backlog tickets",
        "reason": "Promotion is an explicit human gate; automation would bypass the review intent"
      }
    ],
    "must_not_break": [
      "Existing pipeline/run.py stage sequencing and exit behavior \u2014 board.md updates must not cause pipeline failure",
      "Existing pipeline/intake/<uuid>.json schema \u2014 promoted tickets must be identical in format to create_intake.py output",
      "Existing validate_intake.py validation rules \u2014 promote.py reuses them, does not bypass or weaken them",
      "Existing scribe/notes.py get <id> interface \u2014 draft_ticket.py calls it the same way create_intake.py does",
      "Pipeline ID validation and path traversal protections in run.py"
    ]
  },
  "acceptance_criteria": [
    "Given no pipeline/backlog/ directory exists, when draft_ticket.py is run with --title 'My Feature' --problem '...' --description '...' --scope '...', then pipeline/backlog/my-feature.json is created with all fields populated and the script prints the file path to stdout",
    "Given a scribe note with ID 42 exists, when draft_ticket.py is run with --from-todo 42 --title 'From Note' --scope '...', then the note content is used as the description field in the created backlog JSON and the source field is set to '42'",
    "Given --from-todo references note ID 999 which does not exist, when draft_ticket.py runs, then it prints 'Error: note 999 not found' to stderr and exits with code 1",
    "Given draft_ticket.py is run without --title, when argparse processes the args, then it prints a usage error to stderr and exits with code 2",
    "Given pipeline/backlog/my-feature.json already exists, when draft_ticket.py is run with --title 'My Feature', then it prints 'Error: backlog ticket my-feature.json already exists' to stderr and exits with code 1",
    "Given a title with special characters like 'Fix: The $100 (Bug)!!', when draft_ticket.py slugifies it, then the filename is 'fix-the-100-bug.json' with non-alphanumeric chars stripped and consecutive hyphens deduplicated",
    "Given a title longer than 60 characters, when draft_ticket.py slugifies it, then the slug is truncated to 60 characters",
    "Given board.md does not exist, when draft_ticket.py creates the first backlog ticket, then board.md is created with a markdown table header (Slug | Title | Status | UUID | Notes) and one data row with status 'backlog'",
    "Given a valid backlog ticket at pipeline/backlog/my-feature.json, when promote.py my-feature is run, then a UUID is assigned, pipeline/intake/<uuid>.json is created with all required intake fields, pipeline/backlog/my-feature.json is deleted, and the board.md row is updated from 'backlog' to 'ready' with the UUID filled in",
    "Given promote.py is run with slug 'nonexistent', when no pipeline/backlog/nonexistent.json exists, then it prints 'Error: backlog ticket nonexistent.json not found' to stderr and exits with code 1",
    "Given a backlog ticket with problem field shorter than 20 characters, when promote.py runs validation, then it prints the validation errors to stderr and exits with code 1 without creating an intake file or modifying board.md",
    "Given a promoted ticket with UUID abc123 in board.md with status 'ready', when run.py pipeline/intake/abc123.json starts and intake validation passes, then the board.md row for abc123 is updated to status 'running'",
    "Given a pipeline run completes all 6 stages successfully for UUID abc123, when run.py finishes, then the board.md row for abc123 is updated to status 'done'",
    "Given a pipeline run fails at any stage for UUID abc123, when run.py exits, then the board.md row for abc123 is updated to status 'failed'",
    "Given board.md does not exist when run.py executes, when run.py attempts to update board status, then it skips the update silently and pipeline execution continues unaffected",
    "Given board.md has no row matching the current intake UUID, when run.py attempts to update board status, then it skips the update silently and pipeline execution continues unaffected",
    "Given a backlog ticket that was already promoted (file deleted), when promote.py is run with that slug, then it prints 'Error: backlog ticket <slug>.json not found' to stderr and exits with code 1",
    "Given a promoted intake file, when its JSON is compared to one created by create_intake.py, then both have identical schema: id, title, problem, description, scope, context, created_at fields with the same types and validation rules",
    "Given a backlog ticket with --from-todo whose note content is only 10 characters, when draft_ticket.py writes the draft, then it succeeds without validation error \u2014 validation is deferred to promote time"
  ],
  "id": "d0cd0b6c-205a-42a0-8d2e-73fda55901c3",
  "intake_id": "d0cd0b6c-205a-42a0-8d2e-73fda55901c3",
  "valid": true
}