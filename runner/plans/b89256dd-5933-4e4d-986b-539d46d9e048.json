{
  "uuid": "b89256dd-5933-4e4d-986b-539d46d9e048",
  "executor_model": "sonnet",
  "spec": {
    "goal": {
      "problem": "scribe/notes.py uses direct JSONL file I/O for all operations. The new folder-per-type storage engine (scribe/store.py from Phase 1) exists but nothing uses it yet.",
      "solution": "Rewire all CLI commands in scribe/notes.py to delegate storage operations to ScribeStore, removing the old JSONL read/write code paths.",
      "value": "Completes the storage switchover so the CLI produces the new folder-per-type layout, enabling migration (Phase 3) and all downstream phases."
    },
    "shape": {
      "components": [
        {
          "name": "ScribeStore integration in main()",
          "description": "Initialize ScribeStore with the resolved state_dir at CLI entry. Pass it to command handlers instead of raw file paths."
        },
        {
          "name": "Rewritten cmd_add",
          "description": "Calls store.add_note() instead of directly appending to notes.jsonl. Handles --content, --type, --session-id, --role, --mission flags as before."
        },
        {
          "name": "Rewritten cmd_get / cmd_list / cmd_done / cmd_update / cmd_archive",
          "description": "Each delegates to the corresponding ScribeStore method. Output format stays identical to current behavior."
        },
        {
          "name": "Rewritten cmd_learn / cmd_learnings / cmd_unlearn",
          "description": "Learning commands delegate to ScribeStore's learning methods. Same CLI surface."
        },
        {
          "name": "Rewritten auto-archive logic",
          "description": "The existing 30-day auto-archive check now calls store.archive_note() which moves entries to archive subfolders instead of a separate archive JSONL."
        },
        {
          "name": "Removed old JSONL helpers",
          "description": "Remove read_jsonl, write_jsonl, make_note, and other direct file I/O functions that are superseded by ScribeStore."
        }
      ],
      "integration_point": "scribe/notes.py \u2014 full rewrite of storage layer, CLI surface unchanged.",
      "pattern": "Existing argparse structure and command dispatch preserved. Only internal implementation changes.",
      "data_flow": "CLI args -> argparse -> command handler -> ScribeStore method -> folder/index/file I/O -> stdout output.",
      "dependencies": "scribe/store.py (Phase 1 must be complete).",
      "files_to_modify": [
        "scribe/notes.py"
      ],
      "related_files": [
        "scribe/store.py",
        "scribe/test_notes.py"
      ]
    },
    "behavior": {
      "input": "Same CLI interface as today: notes.py add --type todo --content '...', notes.py get 306, notes.py list, notes.py done 306, etc.",
      "processing": "1. main() creates ScribeStore(state_dir). 2. Each cmd_* function receives the store instance. 3. cmd_add calls store.add_note(), prints 'Added #N (type)'. 4. cmd_get calls store.get_note(id), prints metadata + content. 5. cmd_list calls store.list_notes() with filters, prints summary table. 6. cmd_done calls store.update_note(id, status='done'). 7. Auto-archive runs on list/add, calling store.archive_note() for notes older than 30 days. 8. Learning commands call store.add_learning/list_learnings/remove_learning.",
      "output": "Identical CLI output format to current implementation. Same exit codes.",
      "error_cases": [
        {
          "trigger": "Note ID not found",
          "behavior": "Print error message, exit 1. Same as current behavior."
        },
        {
          "trigger": "Invalid --type value",
          "behavior": "Argparse rejects it. Add 'general' to the valid choices."
        }
      ],
      "edge_cases": [
        {
          "condition": "get <id> finds note in archive",
          "behavior": "Print with [archived] indicator."
        },
        {
          "condition": "list with no --type",
          "behavior": "Merge all type indexes via store.list_notes(type=None)."
        },
        {
          "condition": "handoff-sessions subcommand",
          "behavior": "Rewire to scan handoffs/ index instead of full JSONL."
        }
      ]
    },
    "boundaries": {
      "in_scope": [
        "Rewire all cmd_* functions to use ScribeStore",
        "Remove old JSONL read/write helpers",
        "Add 'general' to valid note types",
        "Update scribe/test_notes.py to work with new storage",
        "Preserve identical CLI output format"
      ],
      "out_of_scope": [
        "Migration of existing data \u2014 Phase 3",
        "Startup injection changes \u2014 Phase 4",
        "New CLI subcommands (repair, backup) \u2014 Phase 5",
        "Changes to store.py itself \u2014 Phase 1"
      ],
      "must_not_break": [
        "All existing CLI commands produce identical output format",
        "Exit codes unchanged",
        "FileLock concurrency safety",
        "Backfill agent's ability to call notes.py add --type handoff",
        "Wrapup skill's handoff creation flow"
      ]
    },
    "acceptance_criteria": [
      "Given the new store layout, when 'notes.py add --type todo --content test' is run, then a file appears in todos/ with correct index entry and .md content",
      "Given a note exists with id=5, when 'notes.py get 5' is run, then output matches the format: ID, Type, Status, Session, Time, content",
      "Given notes of mixed types, when 'notes.py list' is run, then all are shown sorted by timestamp with type labels",
      "Given a note with id=5, when 'notes.py done 5' is run, then the index entry status changes to 'done'",
      "Given a note older than 30 days, when any list or add command runs, then the note is auto-archived to the archive/ subfolder",
      "Given 'notes.py learn --content test' is run, then a learning entry appears in learnings/ with correct index and .md file",
      "Given 'notes.py list --type todo --search fix' is run, then only todos with 'fix' in summary are shown",
      "Given 'notes.py show 5' is run (alias), then output is identical to 'notes.py get 5'",
      "Given 'notes.py handoff-sessions' is run, then it lists sessions from handoffs/ index"
    ],
    "id": "b89256dd-5933-4e4d-986b-539d46d9e048",
    "valid": true,
    "series": {
      "name": "scribe-v2",
      "phase": 2,
      "total_phases": 5,
      "parent_spec": "bad6cef8-0700-4fa9-999a-6a768dd00ccf"
    }
  },
  "steps": [
    {
      "step_number": 1,
      "type": "modify",
      "description": "Rewire imports, constants, and main() to initialize ScribeStore; add 'general' to VALID_TYPES; remove old JSONL path helpers and locking helpers",
      "action": "modify",
      "files": [
        "scribe/notes.py"
      ],
      "depends_on": [],
      "code_spec": "In scribe/notes.py:\n\n1. Add import at top: `from scribe.store import ScribeStore, TYPE_FOLDERS`\n\n2. Update VALID_TYPES to include 'general':\n   VALID_TYPES = ['todo', 'handoff', 'decision', 'wishlist', 'reference', 'blocker', 'context', 'general']\n\n3. In main(), after resolving the state_dir (the block that currently computes pk, args.notes_path, args.archive_path, args.learnings_path), replace path resolution with ScribeStore initialization:\n   - Keep the _project_key / scribe_state_dir resolution logic to get state_dir\n   - Instead of setting args.notes_path, args.archive_path, args.learnings_path, do:\n     state_dir = scribe_state_dir() or (PROJECTS_DIR / pk)  # fallback for legacy\n     args.store = ScribeStore(state_dir)\n   - Remove the old notes_path/archive_path/learnings_path assignments from args\n\n4. In main(), replace the command dispatch section (dual_lock_commands, notes_commands, learnings_commands with FileLock wrapping) with a simple dispatch dict that calls each cmd_* with args directly (ScribeStore handles its own locking internally):\n   commands = {\n       'add': cmd_add, 'list': cmd_list, 'get': cmd_get, 'show': cmd_get,\n       'done': cmd_done, 'update': cmd_update, 'archive': cmd_archive,\n       'learn': cmd_learn, 'learnings': cmd_learnings, 'unlearn': cmd_unlearn,\n       'handoff-sessions': cmd_handoff_sessions,\n       'migrate': cmd_migrate,\n   }\n   handler = commands.get(args.command)\n   if handler:\n       handler(args)\n   else:\n       parser.print_help()\n\n5. Remove the following now-unused functions entirely:\n   - read_jsonl\n   - _write_jsonl\n   - _append_jsonl\n   - _raw_max_id and _ID_TOKEN_RE\n   - _next_id\n   - make_note\n   - make_learning\n   - _notes_and_archive_lock\n   - notes_path (the function)\n   - archive_path (the function)\n   - learnings_path (the function)\n\n6. Remove the `from core.utils.filelock import FileLock` import (ScribeStore handles locking). Keep the `from core.session import SessionId` import.\n\n7. Remove the `from contextlib import contextmanager` import (was only for _notes_and_archive_lock).\n\n8. Keep: _now_iso, _parse_timestamp, format_age, _load_session_identity, _project_key, _use_repo_layout, _git_repo_root, _repo_scribe_dir, scribe_state_dir, _parse_id_arg, CLAUDE_DIR, PROJECTS_DIR, STATE_LAYOUT_ENV, APIARY_STATE_DIRNAME, SCRIBE_SUBDIR, AUTO_ARCHIVE_DAYS, MAX_CONTENT_LENGTH, MAX_LAST.\n\n9. Keep run_auto_archive as a public function but rewrite it to use ScribeStore (done in step 5 below).\n\n10. Update the 'add' subparser to use choices=VALID_TYPES (which now includes 'general')."
    },
    {
      "step_number": 2,
      "type": "modify",
      "description": "Rewrite cmd_add to delegate to ScribeStore.add_note()",
      "action": "modify",
      "files": [
        "scribe/notes.py"
      ],
      "depends_on": [
        1
      ],
      "code_spec": "Replace cmd_add(args) with:\n\ndef cmd_add(args):\n    store = args.store\n\n    # Duplicate handoff prevention\n    if getattr(args, 'if_no_handoff_for', None):\n        try:\n            target_sid = SessionId(args.if_no_handoff_for)\n        except ValueError as e:\n            print(f'Error: {e}', file=sys.stderr)\n            return\n        # Search existing handoffs in store\n        handoffs = store.list_notes(note_type='handoff')\n        for h in handoffs:\n            if target_sid.matches(h.get('session', '')):\n                print(f\"Handoff for session {target_sid.short} already exists (#{h['id']}). Skipping.\")\n                return\n\n    content = args.content\n    if len(content.encode('utf-8')) > MAX_CONTENT_LENGTH:\n        print(f'Error: content exceeds {MAX_CONTENT_LENGTH} bytes', file=sys.stderr)\n        sys.exit(1)\n\n    # Build metadata dict for extra fields\n    metadata = {}\n    if getattr(args, 'auto', False):\n        metadata['auto_generated'] = True\n    if getattr(args, 'role', ''):\n        metadata['role'] = args.role\n    if getattr(args, 'mission', ''):\n        metadata['mission'] = args.mission\n\n    entry = store.add_note(\n        note_type=args.type,\n        content=content,\n        session_id=args.session_id or '',\n        **metadata,\n    )\n    print(f\"Added #{entry['id']} ({entry['type']})\")\n\n    # Run auto-archive after add\n    _run_auto_archive_store(store)"
    },
    {
      "step_number": 3,
      "type": "modify",
      "description": "Rewrite cmd_list to delegate to ScribeStore.list_notes() with filtering and auto-archive",
      "action": "modify",
      "files": [
        "scribe/notes.py"
      ],
      "depends_on": [
        2
      ],
      "code_spec": "Replace cmd_list(args) with:\n\ndef cmd_list(args):\n    store = args.store\n\n    # Determine status filter\n    if args.archive:\n        status = 'archived'\n        source = 'archive'\n    else:\n        status = 'all' if args.all else 'active'\n        source = 'active'\n\n    # Run auto-archive before listing active notes (only for unfiltered queries)\n    if not args.archive and not args.search and not args.type and not args.session and not args.role and not args.mission:\n        archived_count = _run_auto_archive_store(store)\n        if archived_count:\n            print(f'[auto-archived {archived_count} notes]', file=sys.stderr)\n\n    # Get notes from store\n    note_type = args.type if args.type else None\n    notes_list = store.list_notes(note_type=note_type, status=status, search=args.search)\n\n    # Filter by status (hide done unless --all or --archive)\n    if not args.all and not args.archive:\n        notes_list = [n for n in notes_list if n.get('status') != 'done']\n\n    # Filter by session\n    if args.session:\n        try:\n            sid = SessionId(args.session)\n        except ValueError as e:\n            print(f'Error: {e}', file=sys.stderr)\n            return\n        notes_list = [n for n in notes_list if sid.matches(n.get('session', ''))]\n\n    # Filter by role\n    if args.role:\n        notes_list = [n for n in notes_list if n.get('role', '').lower() == args.role.lower()]\n\n    # Filter by mission\n    if args.mission:\n        notes_list = [n for n in notes_list if n.get('mission', '').lower() == args.mission.lower()]\n\n    # Limit\n    if args.last is not None:\n        if args.last <= 0:\n            print('Error: --last must be a positive integer', file=sys.stderr)\n            sys.exit(1)\n        last = min(args.last, MAX_LAST)\n        notes_list = notes_list[-last:]\n\n    if not notes_list:\n        print(f'No {source} notes found.')\n        return\n\n    for n in notes_list:\n        nid = n.get('id', '?')\n        ntype = n.get('type', '?')[:8]\n        age = format_age(n.get('timestamp', ''))\n        status_label = ' [DONE]' if n.get('status') == 'done' else ''\n        # For store entries, content is in 'summary' field; read full content for display\n        content = n.get('summary', '').replace('\\n', ' ')[:80]\n        line = f'#{nid:<4} {ntype:<10} ({age:<9}) {content}{status_label}'\n        print(line.encode('ascii', errors='replace').decode('ascii'))"
    },
    {
      "step_number": 4,
      "type": "modify",
      "description": "Rewrite cmd_get to delegate to ScribeStore.get_note() and get_learning()",
      "action": "modify",
      "files": [
        "scribe/notes.py"
      ],
      "depends_on": [
        3
      ],
      "code_spec": "Replace cmd_get(args) with:\n\ndef cmd_get(args):\n    store = args.store\n    raw_id, force_learning = _parse_id_arg(args.id)\n    source_label = None\n    is_learning = False\n\n    if force_learning:\n        note = store.get_learning(raw_id)\n        is_learning = True\n    else:\n        note = store.get_note(raw_id)\n        if note and note.get('status') == 'archived':\n            source_label = '[from archive]'\n\n    if source_label:\n        print(source_label)\n\n    if not note:\n        print(f'Note #{args.id} not found.', file=sys.stderr)\n        sys.exit(1)\n\n    print(f\"ID: {note['id']}\")\n    if is_learning:\n        print('Type: learning')\n    else:\n        print(f\"Type: {note.get('type', '?')}\")\n        print(f\"Status: {note.get('status', '?')}\")\n    print(f\"Session: {note.get('session', '?')}\")\n    print(f\"Time: {note.get('timestamp', '?')} ({format_age(note.get('timestamp', ''))})\")\n    if note.get('role'):\n        print(f\"Role: {note['role']}\")\n    if note.get('mission'):\n        print(f\"Mission: {note['mission']}\")\n    if not is_learning:\n        print(f\"Auto: {note.get('auto_generated', False)}\")\n    print('---')\n    print(note.get('content', ''))"
    },
    {
      "step_number": 5,
      "type": "modify",
      "description": "Rewrite cmd_done, cmd_update, cmd_archive, and run_auto_archive to use ScribeStore",
      "action": "modify",
      "files": [
        "scribe/notes.py"
      ],
      "depends_on": [
        4
      ],
      "code_spec": "Replace cmd_done(args) with:\n\ndef cmd_done(args):\n    store = args.store\n    note = store.get_note(args.id)\n    if not note:\n        print(f'Note #{args.id} not found.', file=sys.stderr)\n        sys.exit(1)\n    if note.get('status') == 'done':\n        print(f'Note #{args.id} is already marked done.')\n        return\n    store.update_note(args.id, status='done')\n    print(f'Marked #{args.id} as done.')\n\nReplace cmd_update(args) with:\n\ndef cmd_update(args):\n    if args.content is None and args.session_id is None:\n        print('Error: provide --content and/or --session-id', file=sys.stderr)\n        sys.exit(1)\n    if args.content is not None and len(args.content.encode('utf-8')) > MAX_CONTENT_LENGTH:\n        print(f'Error: content exceeds {MAX_CONTENT_LENGTH} bytes', file=sys.stderr)\n        sys.exit(1)\n    store = args.store\n    note = store.get_note(args.id)\n    if not note:\n        print(f'Note #{args.id} not found.', file=sys.stderr)\n        sys.exit(1)\n    if note.get('status') == 'done':\n        print(f'Error: note #{args.id} is already done and cannot be updated.', file=sys.stderr)\n        sys.exit(1)\n    kwargs = {}\n    if args.content is not None:\n        kwargs['content'] = args.content\n        kwargs['summary'] = args.content[:120].replace('\\n', ' ').strip()\n    if args.session_id is not None:\n        kwargs['session'] = args.session_id\n    store.update_note(args.id, **kwargs)\n    print(f'Updated #{args.id}.')\n\nReplace cmd_archive(args) with:\n\ndef cmd_archive(args):\n    store = args.store\n    if args.before:\n        try:\n            cutoff = datetime.strptime(args.before, '%Y-%m-%d').replace(tzinfo=timezone.utc)\n        except ValueError:\n            print(f\"Error: --before must be in YYYY-MM-DD format, got {args.before!r}\", file=sys.stderr)\n            sys.exit(1)\n    else:\n        cutoff = datetime.now(timezone.utc) - timedelta(days=AUTO_ARCHIVE_DAYS)\n\n    all_notes = store.list_notes(status='active')\n    archived_count = 0\n    for n in all_notes:\n        ts = _parse_timestamp(n.get('timestamp', ''))\n        archivable = (n.get('status') == 'done') or (n.get('type') == 'handoff')\n        if ts is not None and ts < cutoff and archivable:\n            store.archive_note(n['id'])\n            archived_count += 1\n    if not archived_count:\n        print('Nothing to archive.')\n        return\n    print(f\"Archived {archived_count} notes (before {cutoff.strftime('%Y-%m-%d')}).\")\n\nAdd new helper _run_auto_archive_store and replace run_auto_archive:\n\ndef _run_auto_archive_store(store) -> int:\n    \"\"\"Run auto-archive using ScribeStore. Returns count of archived notes.\n\n    Retention rules by type:\n      handoff  - keep only the latest per role/mission, archive the rest\n      context  - archive after 3 days\n      done     - archive after 1 day (any type marked done)\n      todo/decision/wishlist/blocker - keep until done\n    \"\"\"\n    now = datetime.now(timezone.utc)\n    context_cutoff = now - timedelta(days=3)\n    done_cutoff = now - timedelta(days=1)\n\n    all_notes = store.list_notes(status='active')\n\n    # Find the latest handoff per role/mission\n    latest_handoff = {}  # (role, mission) -> newest timestamp\n    for n in all_notes:\n        if n.get('type') == 'handoff':\n            key = (n.get('role', 'user'), n.get('mission', 'general'))\n            ts = _parse_timestamp(n.get('timestamp', ''))\n            if ts is not None:\n                if key not in latest_handoff or ts > latest_handoff[key]:\n                    latest_handoff[key] = ts\n\n    to_archive_ids = []\n    for n in all_notes:\n        ts = _parse_timestamp(n.get('timestamp', ''))\n        ntype = n.get('type', '')\n        if ts is None:\n            continue\n        if n.get('status') == 'done' and ts < done_cutoff:\n            to_archive_ids.append(n['id'])\n        elif ntype == 'handoff':\n            key = (n.get('role', 'user'), n.get('mission', 'general'))\n            if ts < latest_handoff.get(key, ts):\n                to_archive_ids.append(n['id'])\n        elif ntype == 'context' and ts < context_cutoff:\n            to_archive_ids.append(n['id'])\n\n    for nid in to_archive_ids:\n        store.archive_note(nid)\n    return len(to_archive_ids)\n\n\ndef run_auto_archive(project_key: str, *, start: Path | None = None) -> int:\n    \"\"\"Run auto-archive for a project. Returns count of archived notes.\"\"\"\n    sd = scribe_state_dir(start)\n    if sd is None:\n        sd = PROJECTS_DIR / project_key\n    store = ScribeStore(sd)\n    return _run_auto_archive_store(store)"
    },
    {
      "step_number": 6,
      "type": "modify",
      "description": "Rewrite cmd_learn, cmd_learnings, cmd_unlearn, and cmd_handoff_sessions to use ScribeStore",
      "action": "modify",
      "files": [
        "scribe/notes.py"
      ],
      "depends_on": [
        5
      ],
      "code_spec": "Replace cmd_learn(args) with:\n\ndef cmd_learn(args):\n    store = args.store\n    content = args.content\n    if len(content.encode('utf-8')) > MAX_CONTENT_LENGTH:\n        print(f'Error: content exceeds {MAX_CONTENT_LENGTH} bytes', file=sys.stderr)\n        sys.exit(1)\n    metadata = {}\n    if getattr(args, 'role', ''):\n        metadata['role'] = args.role\n    if getattr(args, 'mission', ''):\n        metadata['mission'] = args.mission\n    entry = store.add_learning(\n        content=content,\n        session_id=args.session_id or '',\n        **metadata,\n    )\n    print(f\"Learned #L{entry['id']}\")\n\nReplace cmd_learnings(args) with:\n\ndef cmd_learnings(args):\n    store = args.store\n    learnings = store.list_learnings(search=args.search)\n\n    if args.role:\n        learnings = [l for l in learnings if l.get('role', '').lower() == args.role.lower()]\n    if args.mission:\n        learnings = [l for l in learnings if l.get('mission', '').lower() == args.mission.lower()]\n\n    if not learnings:\n        print('No learnings found.')\n        return\n\n    for l in learnings:\n        lid = l.get('id', '?')\n        age = format_age(l.get('timestamp', ''))\n        if args.full:\n            # Read full content from store\n            full = store.get_learning(lid)\n            content = full.get('content', '') if full else l.get('summary', '')\n            print(f'#L{lid}: {content}')\n        else:\n            short = l.get('summary', '').replace('\\n', ' ')[:80]\n            print(f'#L{lid:<3} ({age:<9}) {short}')\n\nReplace cmd_handoff_sessions(args) with:\n\ndef cmd_handoff_sessions(args):\n    store = args.store\n    handoffs = store.list_notes(note_type='handoff')\n    seen = set()\n    for n in handoffs:\n        if n.get('status') != 'done':\n            sid = n.get('session', '').strip()\n            if sid and sid not in seen:\n                seen.add(sid)\n                print(sid)\n    if not seen:\n        print('(none)')\n\nReplace cmd_unlearn(args) with:\n\ndef cmd_unlearn(args):\n    store = args.store\n    raw_id, _ = _parse_id_arg(args.id)\n    result = store.remove_learning(raw_id)\n    if result is None:\n        print(f'Learning #L{raw_id} not found.', file=sys.stderr)\n        sys.exit(1)\n    print(f'Removed learning #L{raw_id}.')"
    },
    {
      "step_number": 7,
      "type": "modify",
      "description": "Remove cmd_migrate (out of scope for Phase 2, migration is Phase 3) or keep as no-op stub",
      "action": "modify",
      "files": [
        "scribe/notes.py"
      ],
      "depends_on": [
        6
      ],
      "code_spec": "Replace cmd_migrate(args) with a stub that prints an error directing to Phase 3:\n\ndef cmd_migrate(args):\n    \"\"\"Migration from old format \u2014 deferred to Phase 3.\"\"\"\n    print('Error: migrate command is not yet available (planned for Phase 3).', file=sys.stderr)\n    sys.exit(1)\n\nAlso remove the 're' import if it's no longer used (check: _PROJECT_KEY_RE still uses it, so keep re). Remove the 'os' import if no longer used (check: _use_repo_layout uses os.environ, so keep os). Remove 'subprocess' import if no longer used (check: _git_repo_root uses it, so keep subprocess)."
    },
    {
      "step_number": 8,
      "type": "modify",
      "description": "Rewrite scribe/test_notes.py to test against ScribeStore-backed CLI commands using unittest",
      "action": "modify",
      "files": [
        "scribe/test_notes.py"
      ],
      "depends_on": [
        7
      ],
      "code_spec": "Rewrite test_notes.py using unittest.TestCase. Each test creates a tempdir, initializes ScribeStore, and tests via the rewritten cmd_* functions or directly via store.\n\nStructure:\n\nimport json\nimport sys\nimport tempfile\nimport unittest\nfrom datetime import datetime, timezone, timedelta\nfrom pathlib import Path\n\nsys.path.insert(0, str(Path(__file__).parent.parent))\n\nimport scribe.notes as notes\nfrom scribe.store import ScribeStore\n\n\nclass TestScribeNotes(unittest.TestCase):\n\n    def setUp(self):\n        self._tmp = tempfile.TemporaryDirectory()\n        self.tmp_dir = Path(self._tmp.name)\n        self.store = ScribeStore(self.tmp_dir)\n\n    def tearDown(self):\n        self._tmp.cleanup()\n\n    def _make_args(self, **kwargs):\n        \"\"\"Create a namespace object with store set.\"\"\"\n        ns = type('Args', (), {'store': self.store, **kwargs})()\n        return ns\n\n    def test_add_creates_file_and_index(self):\n        entry = self.store.add_note('todo', 'test content', 'sess1')\n        self.assertEqual(entry['type'], 'todo')\n        self.assertIn('id', entry)\n        # Verify file exists in todos/\n        md_path = self.tmp_dir / 'todos' / f\"{entry['id']}.md\"\n        self.assertTrue(md_path.exists())\n        self.assertEqual(md_path.read_text(encoding='utf-8'), 'test content')\n\n    def test_add_general_type(self):\n        # 'general' is now a valid type\n        self.assertIn('general', notes.VALID_TYPES)\n        entry = self.store.add_note('general', 'general note', 'sess1')\n        self.assertEqual(entry['type'], 'general')\n\n    def test_get_note(self):\n        entry = self.store.add_note('todo', 'get me', 'sess1')\n        result = self.store.get_note(entry['id'])\n        self.assertIsNotNone(result)\n        self.assertEqual(result['content'], 'get me')\n        self.assertEqual(result['type'], 'todo')\n\n    def test_get_note_from_archive(self):\n        entry = self.store.add_note('todo', 'archive me', 'sess1')\n        self.store.archive_note(entry['id'])\n        result = self.store.get_note(entry['id'])\n        self.assertIsNotNone(result)\n        self.assertEqual(result['status'], 'archived')\n\n    def test_list_notes_all_types(self):\n        self.store.add_note('todo', 'A', 'sess1')\n        self.store.add_note('handoff', 'B', 'sess1')\n        self.store.add_note('decision', 'C', 'sess1')\n        result = self.store.list_notes()\n        self.assertEqual(len(result), 3)\n\n    def test_list_notes_type_filter(self):\n        self.store.add_note('todo', 'A', 'sess1')\n        self.store.add_note('handoff', 'B', 'sess1')\n        result = self.store.list_notes(note_type='todo')\n        self.assertEqual(len(result), 1)\n        self.assertEqual(result[0]['type'], 'todo')\n\n    def test_list_notes_search_filter(self):\n        self.store.add_note('todo', 'fix the bug', 'sess1')\n        self.store.add_note('todo', 'add feature', 'sess1')\n        result = self.store.list_notes(search='fix')\n        self.assertEqual(len(result), 1)\n\n    def test_done_updates_status(self):\n        entry = self.store.add_note('todo', 'do this', 'sess1')\n        self.store.update_note(entry['id'], status='done')\n        result = self.store.get_note(entry['id'])\n        self.assertEqual(result['status'], 'done')\n\n    def test_archive_note(self):\n        entry = self.store.add_note('todo', 'archive target', 'sess1')\n        archived = self.store.archive_note(entry['id'])\n        self.assertIsNotNone(archived)\n        self.assertEqual(archived['status'], 'archived')\n        # Should not appear in active list\n        active = self.store.list_notes(status='active')\n        self.assertEqual(len(active), 0)\n        # Should appear in archived list\n        arch_list = self.store.list_notes(status='archived')\n        self.assertEqual(len(arch_list), 1)\n\n    def test_auto_archive_old_done_notes(self):\n        # Add note and manually backdate its index entry\n        entry = self.store.add_note('todo', 'old done note', 'sess1')\n        old_ts = (datetime.now(timezone.utc) - timedelta(days=2)).isoformat()\n        self.store.update_note(entry['id'], status='done', timestamp=old_ts)\n        count = notes._run_auto_archive_store(self.store)\n        self.assertGreaterEqual(count, 1)\n\n    def test_learn_creates_entry(self):\n        entry = self.store.add_learning('discovered a trick', 'sess1')\n        self.assertIn('id', entry)\n        md_path = self.tmp_dir / 'learnings' / f\"{entry['id']}.md\"\n        self.assertTrue(md_path.exists())\n\n    def test_list_learnings(self):\n        self.store.add_learning('learning one', 'sess1')\n        self.store.add_learning('learning two', 'sess1')\n        result = self.store.list_learnings()\n        self.assertEqual(len(result), 2)\n\n    def test_list_learnings_search(self):\n        self.store.add_learning('fix encoding', 'sess1')\n        self.store.add_learning('add feature', 'sess1')\n        result = self.store.list_learnings(search='fix')\n        self.assertEqual(len(result), 1)\n\n    def test_unlearn_removes_entry(self):\n        entry = self.store.add_learning('remove me', 'sess1')\n        removed = self.store.remove_learning(entry['id'])\n        self.assertIsNotNone(removed)\n        result = self.store.list_learnings()\n        self.assertEqual(len(result), 0)\n\n    def test_format_age(self):\n        now = datetime.now(timezone.utc)\n        self.assertEqual(notes.format_age(now.strftime('%Y-%m-%dT%H:%M:%SZ')), 'just now')\n        five_min = (now - timedelta(minutes=5)).strftime('%Y-%m-%dT%H:%M:%SZ')\n        self.assertEqual(notes.format_age(five_min), '5m ago')\n\n    def test_handoff_sessions(self):\n        self.store.add_note('handoff', 'handoff 1', 'sess-abc')\n        self.store.add_note('handoff', 'handoff 2', 'sess-def')\n        handoffs = self.store.list_notes(note_type='handoff')\n        sessions = {h.get('session', '') for h in handoffs if h.get('status') != 'done'}\n        self.assertEqual(len(sessions), 2)\n\n    def test_show_alias_same_as_get(self):\n        # Verify 'show' is registered as alias in argparse (covered by 'aliases=[\"show\"]')\n        # Both should call cmd_get \u2014 just verify cmd_get works\n        entry = self.store.add_note('todo', 'show me', 'sess1')\n        result = self.store.get_note(entry['id'])\n        self.assertIsNotNone(result)\n        self.assertEqual(result['content'], 'show me')\n\n\nif __name__ == '__main__':\n    unittest.main()"
    },
    {
      "step_number": 9,
      "type": "test",
      "description": "Run the rewritten test suite to verify all tests pass",
      "action": "test",
      "files": [
        "scribe/test_notes.py"
      ],
      "depends_on": [
        8
      ],
      "code_spec": "python -m unittest scribe.test_notes"
    },
    {
      "step_number": 10,
      "type": "test",
      "description": "Run the Phase 1 store tests to confirm no regressions",
      "action": "test",
      "files": [
        "scribe/test_store.py"
      ],
      "depends_on": [
        7
      ],
      "code_spec": "python -m unittest scribe.test_store"
    },
    {
      "step_number": 11,
      "type": "verify",
      "description": "Verify all acceptance criteria are met: CLI surface unchanged, store-backed operations work, identical output format",
      "action": "verify",
      "files": [
        "scribe/notes.py",
        "scribe/test_notes.py"
      ],
      "depends_on": [
        9,
        10
      ],
      "code_spec": "Verify the following acceptance criteria:\n1. 'general' is in VALID_TYPES and the add subparser accepts it as a choice.\n2. cmd_add calls store.add_note() and prints 'Added #N (type)' \u2014 no direct JSONL writes remain.\n3. cmd_get calls store.get_note() and prints ID/Type/Status/Session/Time/content format.\n4. cmd_list calls store.list_notes() with filters, prints summary table with same format.\n5. cmd_done calls store.update_note(id, status='done').\n6. Auto-archive uses _run_auto_archive_store which calls store.archive_note() for old notes.\n7. Learning commands (learn, learnings, unlearn) all delegate to ScribeStore methods.\n8. cmd_handoff_sessions scans handoffs via store.list_notes(note_type='handoff').\n9. 'show' alias maps to cmd_get.\n10. No references to read_jsonl, _write_jsonl, _append_jsonl, make_note, make_learning remain in the command functions.\n11. All tests pass with unittest.\n12. No external dependencies used \u2014 stdlib only.\n13. All file I/O uses encoding='utf-8' (handled by ScribeStore).\n14. Exit codes: 0 on success, 1 on expected failure (note not found, invalid input)."
    }
  ],
  "valid": true
}