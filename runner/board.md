| Slug | Title | Status | UUID | Notes |
|------|-------|--------|------|-------|
| gitignore-runner-runtime-artifact-directories | Gitignore runner runtime artifact directories | done | dc1ed814-3316-488b-83ce-63966ffdfe3b | |
| pretooluse-hook-enforcing-cli-lookup-before-repo-cli-use | PreToolUse hook enforcing cli_lookup before repo CLI use | done | c7958c29-6650-40be-99cb-9935f9c35de8 | step 3 completed manually after executor's git-commit step failed silently — see follow-up tickets for executor stdout/stderr handling and run.py truncation |
| executor-surfaces-git-stdout-on-commit-failures | Executor surfaces git stdout on commit failures | done | | hand-fixed manually, not via runner |
| runner-run-py-shows-full-stage-stderr-on-failure | Runner run.py shows full stage stderr on failure | done | | hand-fixed manually, not via runner |
| make-claude-apiary-fully-portable-across-machines-and-oses | Make claude-apiary fully portable across machines and OSes | done |  | split into T5a-T5d phases 2026-04-07 |
| document-portability-rules-in-memory-notes-and-claude-md | Document portability rules in memory notes and CLAUDE.md | done |  | memory files + MEMORY.md index + CLAUDE.md Portability section + scribe notes #174/#175 + settings.json audit appended to portability epic context. Scope item 8 (/dev/null line) skipped — that string lives in the harness env block, not user CLAUDE.md. |
| add-request-id-grouping-to-budgeter-for-multi-task-chains | Add request_id grouping to budgeter for multi-task chains | done |  | log_agent_cost.py --request-id flag, runner/run.py threads runner_uuid as request_id, report.py --by-request view. Smoke-tested end-to-end. Backward compat: pre-existing entries bucket into (no request). |
| overnight-cron-runner-with-morning-branch-review-workflow | Overnight cron runner with morning branch review workflow | done | 59b6da98-a0f2-4a3e-8328-e5cecb18a1d8 | Runner ran 6/6 stages; auto_harden surfaced 21 findings. All 6 critical+high fixed in 83a7d97 (ATK-002/003/004/005/008/010) plus ATK-001/009/016 in passing. Remaining mediums/lows deferred — see TODO #198. |
| runner-mark-done-cli-for-hand-fixed-tickets | Runner mark-done CLI for hand-fixed tickets | done |  | self-hosted: closed itself after build + smoke test |
| refine-to-plan-file-handoff-to-cut-auto-plan-token-cost | Refine-to-plan file handoff to cut auto_plan token cost | backlog | | |
| runner-executor-architecture-hardening-from-t4-failures | Runner executor architecture hardening from T4 failures | done |  | hand-fixed manually, not via runner. Items A/B/C/D landed in 631c69f, 04635f1, 46a2dbc, 9cc0117. 23 unittest cases in runner/test_validate_plan.py + runner/test_executor.py. |
| portability-t5a-settings-json-paths-and-interpreter-normaliz | Portability T5a: settings.json paths and interpreter normalization | done | b1bb662c-12e7-4d2f-aa2e-ff8def40dc75 | hand-fixed after runner failed 3x (see scribe blockers #203/#206/#209). In-repo .claude/settings.json now uses $CLAUDE_PROJECT_DIR; requirements.txt added (pytest only); README.md Requirements section declares Python >=3.11; codebase already 3.11-compatible. core/hooks/*.py audit: no changes needed — all use Path(__file__) for apiary-root resolution, and the only cwd usages are user-project-cwd which the spec says to leave alone. ~/.claude/settings.json rewrite still pending as note #205. |
| portability-t5b-cross-platform-shell-hygiene-audit | Portability T5b: cross-platform shell hygiene audit | done | b6fdfaf7-0edb-43ad-9a01-6e8d074d17c3 | Hand-fixed on master (2026-04-07). Runner failed twice: first on a planner bug (step 3 was a type:test step whose description said 'expected to report violations' — self-contradictory gate), second with executor reporting 'subprocess made no changes' on step 4 because the plan listed 16 runner/*.py files that had zero violations. Audit script actually found only 5 violations, not 46 as spec claimed. Hand-fixes: grabbed step 1 (executor.py shell=True → shlex.split) and step 2 (scripts/audit_portability.py) from runner/b6fdfaf7 branch; added encoding='utf-8' to 4 sites (budgeter/lib/logger.py:39, budgeter/test_hooks.py:47+63, core/utils/filelock.py:21); reworded CLAUDE.md Portability null-device bullet to shell-agnostic prose. Verified: audit reports 0 violations; 25/25 relevant tests pass (runner.test_executor, runner.test_validate_plan, budgeter.test_hooks). Planner bugs filed as scribe TODOs #211 (failure-expected test steps) and #212 (out-of-repo absolute paths in plan files). Skipped scripts/smoke_cli_tools.py and runner/test_audit_portability.py — nice-to-haves not worth a second runner run. |
| portability-t5c-bootstrap-script-and-stable-project-key | Portability T5c: bootstrap script and stable project key | done |  | Hand-written on master 2026-04-07 (commit 9675002). Stable project key via .claude-project-key file (value: claude-apiary) with regex-validated fallback to legacy cwd-derived key. scripts/bootstrap.py idempotent; scripts/migrate_project_key.py moves only scribe-owned files, leaves harness transcripts in place. Migration executed on this machine: D--Professional-claude-apiary → claude-apiary. notes.py list returns same 47-entry set from new location. All 3 consumers (core/startup.py, core/hooks/startup_hook.py, scribe/notes.py) switched to get_project_key(); enforce_cli_lookup.py intentionally keeps cwd-derived key because it reads harness transcripts. |
| portability-t5d-portability-docs-and-fresh-vm-validation | Portability T5d: PORTABILITY docs and fresh-VM validation | done |  | Hand-written on master 2026-04-07 (commit f012a05). PORTABILITY.md at repo root covers prereqs/bootstrap/state-locality/contributor rules/troubleshooting. README.md broken CLAUDE.md link repointed. bootstrap.py MIN_PYTHON bumped 3.9→3.11 to match README. State-locality decision: scribe notes/learnings/memory and budgeter logs are intentionally per-machine and not portable — no export/import scripts. Validation: throwaway-HOME bootstrap passes (fresh seed, re-run idempotency, migrate no-op on empty HOME). Beefier clone-to-tempdir validation also passes. True fresh-VM test is filed as scribe note #214 for out-of-band execution — not runnable from inside Claude Code. |
| backfill-skip-state-file-for-unseen-sessions | backfill_skip state file for unseen_sessions | done | 17f237c7-f227-4553-ad58-3692b4f10bc3 | Hand-finished on master (2026-04-07). Runner passed validate/refine/plan (3/6), failed executor step 1: the step targeted ~/.claude/projects/claude-apiary/backfill_skip.json (outside the repo, per our portability rule that persistent state lives under ~/.claude/projects/claude-apiary/). The subprocess wrote the file correctly, but the executor's git-based "expected files changed" verifier cannot see out-of-repo paths and aborted, skipping steps 2-5. Hand-applied step 2 (BACKFILL_SKIP_PATH constant, load_skip_prefixes() helper, `handoff_sids \|= load_skip_prefixes()` union) and step 3 (core/test_startup_skip.py, 16 tests, all pass). Verified: unseen_sessions list dropped from 8 historical sessions to 0. Related to scribe TODO #212 (planner/executor out-of-repo paths). |
